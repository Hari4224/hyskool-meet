version: '3.8'

services:
  hyskool-meet:
    build: .
    container_name: hyskool_meet_server
    restart: always
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=production
      - JWT_SECRET=hyskool_meet_super_secret_key_2026
